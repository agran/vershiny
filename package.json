{
  "name": "vershiny",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Open-source аналог PeakFinder: панорама горизонта и подписи вершин (PWA, офлайн-first)",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
