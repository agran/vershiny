# Генерируемое и стороннее: форматирование там создаёт ложный шум в диффах
dist/
node_modules/
public/tiles/
public/peaks/
data/
dem/
*.jsonl
package-lock.json

# Документация: таблицы и абзацы в STATUS.md пишутся руками, авто-перенос
# их ломает (уже был разрыв абзаца строкой, начинающейся с «+»)
docs/
